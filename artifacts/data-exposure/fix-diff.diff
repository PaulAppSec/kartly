diff --git a/server/src/services/userService.ts b/server/src/services/userService.ts
index d40a072..8f5c8a6 100644
--- a/server/src/services/userService.ts
+++ b/server/src/services/userService.ts
@@ -7,14 +7,13 @@ import type { AddressInput, UpdateMeInput } from "../schemas/userSchemas.js";
 import { toPublicUser } from "./authService.js";
 
 export const userService = {
-  // ⚠️ VULNERABLE ON PURPOSE (main) — VULNS.md #21 (Sensitive data exposure).
-  // Returns the RAW user row (including `passwordHash`, which is #4's plaintext)
-  // instead of the public DTO. The fix (fix/data-exposure) returns
-  // toPublicUser(user) — an explicit field allowlist.
+  // FIX (fix/data-exposure) — VULNS.md #21. Return an explicit public DTO
+  // (allowlisted fields) instead of the raw row, so passwordHash / tokens / other
+  // internal columns are never serialized to the client.
   async getMe(userId: string) {
     const user = await userRepo.findById(userId);
     if (!user) throw new HttpError(404, "User not found.");
-    return user;
+    return toPublicUser(user);
   },
 
   async updateMe(userId: string, input: UpdateMeInput) {
